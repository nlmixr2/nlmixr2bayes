% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nimbleGen.R
\name{nimbleLinkedSample}
\alias{nimbleLinkedSample}
\title{Sample a linked FOCEi conditional likelihood with NIMBLE (phase 1)}
\usage{
nimbleLinkedSample(
  ui,
  data,
  niter = 2000L,
  nburnin = 1000L,
  thetaSd = NULL,
  likelihood = c("focei", "foce"),
  rxControl = rxode2::rxControl(),
  cores = rxode2::getRxThreads(),
  literalFix = TRUE,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{ui}{rxode2 ui}

\item{data}{estimation data}

\item{niter}{total MCMC iterations per chain}

\item{nburnin}{burn-in iterations}

\item{thetaSd}{prior SD (natural scale) for free thetas; default
`pmax(3 * abs(est), 1)`}

\item{likelihood}{`"focei"` or `"foce"` (see [stanLinkSetup()])}

\item{rxControl}{solving options}

\item{cores}{subject-parallel thread count for each likelihood evaluation}

\item{literalFix}{inline fixed population parameters as literals (kept
matched with the linked problem, as in [stanLinkSetup()])}

\item{seed}{passed to [nimble::runMCMC()]}

\item{verbose}{show NIMBLE's model-compiler output}
}
\value{
the `runMCMC()` samples
}
\description{
Diagonal-omega, no-covariate, no-mixture models only; see the file header
comment for the full scope statement. Reuses [stanLinkSetup()] (and the
same theta-base/mu-reference setup [stanEst.R] uses) verbatim -- that
machinery is engine-agnostic.
}
\author{
Matthew L Fidler
}
